spring:
    profiles:
      active:
        - dev

---
spring:
  profiles: dev
  application:
    name: microservicecloud-config-shf-dev

---
spring:
  profiles: test
  application:
    name: microservicecloud-config-shf-test